--- living_world/engine/simulation.py
+++ living_world/engine/simulation.py
@@ -10,12 +10,14 @@
         self.city = City()
         self.npcs = []
         self.events_log = []
+        self.unsaved_events = []

         bus.subscribe("log_event", self._on_log_event)

     def _on_log_event(self, msg):
         event = {"time": self.time.format_time(), "msg": msg}
         self.events_log.append(event)
-        # Ограничиваем объем RAM (храним только последние 500 для GUI и БД)
-        if len(self.events_log) > 500:
+        self.unsaved_events.append(event)
+        if len(self.events_log) > 200:
             self.events_log.pop(0)

     def add_npc(self, npc):
